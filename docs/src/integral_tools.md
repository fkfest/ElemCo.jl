# Integral tools

Tools for generating integral dumps from AO integrals — both density-fitted
and exact (non-DF) — and transforming them from the AO to the MO basis.

```@docs
ElemCo.IntegralTools
```

## Exported functions

```@autodocs
Modules = [ElemCo.IntegralTools]
Private = false
Order = [:function]
```

## Internal functions
```@autodocs
Modules = [ElemCo.IntegralTools]
Public = false
Order = [:function]
```
