# ± supermatrix and half-transformed AO stores

The persisted exact-AO integral stores: the ± supermatrix store (the only
representation of the exact AO integrals) and the half-transformed
(`⟨iν|ρσ⟩`) store the AO-direct correlated methods read.

```@docs
ElemCo.PMStore
```

## Exported functions

```@autodocs
Modules = [ElemCo.PMStore]
Private = false
Order = [:constant, :type, :function]
```

## Internal functions

```@autodocs
Modules = [ElemCo.PMStore]
Public = false
Order = [:constant, :type, :function]
```
